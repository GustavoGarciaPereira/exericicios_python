### Perguntas Python

1. **Qual é o resultado da expressão `5 + 3 * 2` em Python?**
   - A) 11
   - B) 13
   - C) 16
   - D) 20

2. **Qual é o tipo de dado de uma variável que contém a string "Olá, Mundo!" em Python?**
   - A) Inteiro
   - B) Flutuante
   - C) String
   - D) Booleano

3. **Qual é o resultado da expressão `True and False` em Python?**
   - A) True
   - B) False
   - C) Erro
   - D) Nulo

4. **Qual é o comando para imprimir uma mensagem na tela em Python?**
   - A) `print()`
   - B) `echo()`
   - C) `cout()`
   - D) `console.log()`

5. **Qual é o operador usado para atribuir um valor a uma variável em Python?**
   - A) `==`
   - B) `=`
   - C) `+=`
   - D) `-=`

### Perguntas Django

1. **Qual é a principal diferença entre uma lista e uma tupla em Python?**
   - A) Uma lista é imutável, enquanto uma tupla é mutável.
   - B) Uma lista é mutável, enquanto uma tupla é imutável.
   - C) Uma lista é usada para números, enquanto uma tupla é usada para strings.
   - D) Uma lista é usada para strings, enquanto uma tupla é usada para números.

2. **Qual é o comando para criar um novo projeto Django?**
   - A) `django-admin startproject nome_do_projeto`
   - B) `python manage.py startproject nome_do_projeto`
   - C) `django startproject nome_do_projeto`
   - D) `python startproject nome_do_projeto`

3. **Qual é o propósito do arquivo `settings.py` em um projeto Django?**
   - A) Configurar rotas da aplicação.
   - B) Definir modelos de dados.
   - C) Configurar parâmetros globais do projeto.
   - D) Criar views.

4. **Como você acessa um elemento em uma lista em Python?**
   - A) Usando o nome do elemento.
   - B) Usando o índice do elemento.
   - C) Usando uma função específica.
   - D) Usando uma classe.

5. **Qual é o comando para executar o servidor de desenvolvimento Django?**
   - A) `python manage.py runserver`
   - B) `django runserver`
   - C) `python runserver`
   - D) `django-admin runserver`

